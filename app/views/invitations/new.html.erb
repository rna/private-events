<h1>Send Invitations</h1>

<% @users.each do |user| %>
<ul>
  <li><%= user.name %><%= button_to 'Invite', invitations_path, method: 'post', params:{attendee_id:user.id, event_id:@event.id} %></li>
</ul>
<% end %>

